#include <stdio.h>

int main() {
	int year;
	scanf("%d", &year);
  int x;

	if (year % 400 == 0) x = 1;
	else if (year % 100 == 0) x = 0;
	else if (year % 4 == 0) x = 1;
	else ret = 0;

	printf("%d\n", ret);

	return 0;
}
